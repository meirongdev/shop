server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    include /etc/nginx/mime.types;

    # Correct MIME type for WebAssembly
    types {
        application/wasm wasm;
    }

    # Cache content-hashed WASM bundles aggressively
    location ~* \.[0-9a-f]+\.wasm$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        gzip_static on;
    }

    # JS entry point has no content hash — must revalidate on each deploy
    location ~* \.js$ {
        add_header Cache-Control "no-cache";
        gzip_static on;
    }

    # SPA fallback: serve index.html for any path without a file extension
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }
}
