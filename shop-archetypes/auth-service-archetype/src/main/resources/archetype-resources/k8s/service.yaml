apiVersion: v1
kind: Service
metadata:
  name: ${artifactId}
  labels:
    app: ${artifactId}
spec:
  selector:
    app: ${artifactId}
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: management
      port: 8081
      targetPort: management
